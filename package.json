{
  "name": "kea-disposables",
  "version": "0.0.0",
  "description": "A kea plugin for managing disposable resources",
  "main": "src/index.ts",
  "license": "MIT",
  "scripts": {
    "test": "jest"
  },
  "peerDependencies": {
    "kea": "^3.0.0"
  },
  "devDependencies": {
    "kea": "^3.0.0",
    "jest": "^29.0.0",
    "@babel/core": "^7.0.0",
    "@babel/preset-env": "^7.0.0",
    "@babel/preset-typescript": "^7.0.0",
    "babel-jest": "^29.0.0"
  },
  "packageManager": "pnpm@10.0.0",
  "jest": {
    "testEnvironment": "node",
    "transform": {
      "^.+\\.(js|ts)$": "babel-jest"
    }
  },
  "babel": {
    "presets": [
      "@babel/preset-env",
      "@babel/preset-typescript"
    ]
  }
}
